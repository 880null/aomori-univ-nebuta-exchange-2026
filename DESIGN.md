# Design

多言語ねぶたガイドの視覚システム。PRODUCT.md が製品の事実を持ち、この文書は
持続する視覚上の決定のみを持つ。実装の詳細やコンポーネント単位の寸法は書かない。

## World

**和紙と墨。** ねぶたを規定しているのは、和紙の地と、その上に引かれた太い墨の
輪郭線と、枠の内側から透ける濃彩の染料である。この3つをそのままUIの材料に使う。

- **地** … 和紙の温白色。屋外の直射日光下で本文が読めることを最優先にした結果、
  暗い地は採らない。
- **構造** … 影・角丸・ぼかしではなく**罫線**で作る。日中の屋外では影とぼかしが
  最初に失われるため、輪郭で構造を伝える。
- **色** … 染料の濃彩（藍・緑青・朱・山吹）。装飾ではなく**タグ4軸の識別**という
  機能に限って使う。地に対する面積は小さく保つ。

色戦略は Restrained（地＋墨＋機能色）。利用者は鑑賞ではなく探索に来ており、
Operate 面が主であるため。

## Tokens

durable な役割のみを定める。実際の値は最初の実装で確定し、確定後にここへ反映する。

| 役割 | 用途 |
|---|---|
| `--washi` | 地。温白色 |
| `--sumi` | 本文・見出しの墨色 |
| `--sumi-weak` | 副次情報（読み仮名・制作者・抜粋） |
| `--rule` | 罫線。行の区切りと構造 |
| `--rule-strong` | 節の区切り |
| `--surface` | 行の面。地よりわずかに明るい |
| `--axis-theme` | タグ軸「テーマ」＝藍 |
| `--axis-subject` | タグ軸「題材」＝緑青 |
| `--axis-scene` | タグ軸「場面」＝朱 |
| `--axis-era` | タグ軸「時代・出典」＝山吹 |

すべての文字色は地に対して 4.5:1 以上を満たすこと。屋外の明るい環境が既定条件で
あるため、この下限は交渉の対象にしない。

## Typography

書体は**システムスタック**を使う。6言語（日本語／やさしい日本語／English／
簡体中文／繁體中文／한국어）でCJKとラテン文字が混在し、かつオフライン利用が
要件であるため、Webフォントの追加読み込みは採らない。

- 行間は言語によって最適値が異なる。CJKはラテン文字より広い行間を必要とする。
  本文の行間は 1.7 以上を基準にする。
- 字間は見出しでのみ調整する。CJK本文に負の字間を与えない。
- 通し番号は tabular numerals（等幅数字）で桁を揃える。目録の桁揃えが崩れると
  一覧の走査速度が落ちる。
- 読み仮名は本文より小さく、色を弱めるが、コントラスト下限は守る。

## Composition

**図録の作品目録。** 1行＝1作品の完全なレコードとして組む。カードの集合ではない。

- 掲載条件である**団体名・制作者名・題名の3点は、目録の記載項目として全行に置く**。
  折りたたみ・省略・画像内への埋め込みで代替しない（PRODUCT.md 掲載条件1）。
- 行の中の視線順を固定する: 通し番号 → 原画 → 題名（読み仮名）→ 団体名／制作者
  → 抜粋 → タグ。
- 原画は**小さく置く**。手元のデータは 450×259px であり、これを超える表示幅を
  与えるとぼける。表示幅は元画素をデバイス解像度で割った値を上限とする。
- 一覧の抜粋は**文字数で切らない**。行数で打ち切る。6言語で1文字あたりの情報量が
  異なるため、文字数による切り詰めはいずれかの言語で必ず破綻する。

## Motion

動きは状態の変化を伝えるためだけに使う。装飾的な動きを持たない。

- `prefers-reduced-motion: reduce` を尊重する。この場合、位置と不透明度の変化を
  取り除き、状態の差は色と輪郭だけで表す。
- 触れた瞬間に応答を返す。会場は電波が混雑するため、体感速度が信頼に直結する。
  押下のフィードバックは pointer-down で出し、遅延を挟まない。

## Terminology

- 画像は**「原画」**と表記する。UI文言・見出し・代替テキスト・ファイル名・
  データのキー名すべてに適用する（PRODUCT.md 掲載条件2）。
- **提供された解説本文は一切書き換えない。** 本文中に「下絵」があってもそのまま
  表示する。同条2の但し書きを参照。
- タグ軸の見出しは、スプレッドシートの列名ではなく**中身に合わせた名前**を使う。
  データ側の列名は変更しない。

| CSV列名 | 表示見出し |
|---|---|
| タグ1（テーマ） | テーマ |
| タグ2（メッセージ） | 題材 |
| タグ3（シーン） | 場面 |
| タグ4（時代背景） | 時代・出典 |
