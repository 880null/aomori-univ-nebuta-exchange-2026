# Design

多言語ねぶたガイドの視覚システム。PRODUCT.md が製品の事実を持ち、この文書は
持続する視覚上の決定のみを持つ。実装の詳細やコンポーネント単位の寸法は書かない。

## World

**和紙と墨。** ねぶたを規定しているのは、和紙の地と、その上に引かれた太い墨の
輪郭線と、枠の内側から透ける濃彩の染料である。この3つをそのままUIの材料に使う。

- **地** … 和紙の温白色。屋外の直射日光下で本文が読めることを最優先にした結果、
  暗い地は採らない。
- **構造** … 影・角丸・ぼかしではなく**罫線**で作る。日中の屋外では影とぼかしが
  最初に失われるため、輪郭で構造を伝える。
- **色** … 染料の濃彩（藍・緑青・朱・山吹）。装飾ではなく**タグ4軸の識別**という
  機能に限って使う。地に対する面積は小さく保つ。

色戦略は Restrained（地＋墨＋機能色）。利用者は鑑賞ではなく探索に来ており、
Operate 面が主であるため。

## Tokens

durable な役割のみを定める。実際の値は最初の実装で確定し、確定後にここへ反映する。

| 役割 | 用途 |
|---|---|
| `--washi` | 地。温白色 |
| `--sumi` | 本文・見出しの墨色 |
| `--sumi-weak` | 副次情報（読み仮名・制作者・抜粋） |
| `--rule` | 罫線。行の区切りと構造 |
| `--rule-strong` | 節の区切り |
| `--surface` | 行の面。地よりわずかに明るい |
| `--axis-theme` | タグ軸「テーマ」＝藍 |
| `--axis-subject` | タグ軸「題材」＝緑青 |
| `--axis-scene` | タグ軸「場面」＝朱 |
| `--axis-era` | タグ軸「時代・出典」＝山吹 |

すべての文字色は地に対して 4.5:1 以上を満たすこと。屋外の明るい環境が既定条件で
あるため、この下限は交渉の対象にしない。

## Typography

書体は**システムスタック**を使う。6言語（日本語／やさしい日本語／English／
簡体中文／繁體中文／한국어）でCJKとラテン文字が混在し、かつオフライン利用が
要件であるため、Webフォントの追加読み込みは採らない。

- 行間は言語によって最適値が異なる。CJKはラテン文字より広い行間を必要とする。
  本文の行間は 1.7 以上を基準にする。
- 字間は見出しでのみ調整する。CJK本文に負の字間を与えない。
- 通し番号は tabular numerals（等幅数字）で桁を揃える。目録の桁揃えが崩れると
  一覧の走査速度が落ちる。
- 読み仮名は本文より小さく、色を弱めるが、コントラスト下限は守る。

## Composition

**図録の作品目録。** 1行＝1作品の完全なレコードとして組む。カードの集合ではない。

- 掲載条件である**団体名・制作者名・題名の3点は、目録の記載項目として全行に置く**。
  折りたたみ・省略・画像内への埋め込みで代替しない（PRODUCT.md 掲載条件1）。
- 行の中の視線順を固定する: 通し番号 → 原画 → 題名（読み仮名）→ 団体名／制作者
  → 抜粋。
- **タグは行に置かない。** 行内に4軸のタグを並べた時点で1行294pxに達し、23件で
  約6,800pxのスクロールになった。タグは分類のための道具であって読むものではない。
  絞り込みUIとして画面上部に集約し、行は目録のレコードに徹する（232px／行）。
- 一覧では原画を**小さく置く**（112px）。手元のデータは幅450px固定・高さ204〜313pxで
  個体差があり、これを超える表示幅を与えるとぼける。一覧は走査のための画面なので、
  くっきりしていることを大きさより優先する。
- **画像の内在寸法は1件ずつ実寸を渡す**。共通の固定値を渡すと読み込み時に行が跳ねる。
- **一覧で見える抜粋の量を決めるのは行数であって文字数ではない。** 6言語で1文字あたりの
  情報量が異なるため、見える量を文字数で決めるとどれかの言語で必ず破綻する。
  ビルド時にも抜粋へ文字数の上限を置くが、これは**ペイロード削減のためだけ**にあり、
  **表示可能量の2倍以上の余裕を必ず保つ**。上限が見える範囲に食い込んだ時点で、
  それは行数による打ち切りではなくなる。実測（2行に収まる最大文字数）は
  320px幅で日本語32字・英語66字、1440px幅で日本語62字・英語135字。

## Detail

**その作品を見に来た画面。** 一覧が走査の画面なのに対し、詳細は読ませる画面。
配色・トークン・タイポグラフィは一覧と共有し、組み方だけを読書向けに変える。

縦の順序: 一覧へ戻る → （言語切替）→ 通し番号・題名・読み仮名 → 団体名・制作者名
→ 原画 → 見どころ → 解説 → タグ4軸 → 前後の作品。

- **言語切替は本文より上**。留学生・海外観光客が最初に必要とするのは「読める状態に
  すること」で、題名や団体名より優先される。
- **原画は画面幅いっぱいに出す**（上限640px）。一覧とは逆の判断。素材が450px幅しかなく
  拡大によるぼけが出るが、詳細は作品を見に来た画面であり、小さく正確であることより
  大きく見えることを優先する。**高解像度データが入り次第、ファイルを差し替えるだけで
  済むよう、表示寸法をピクセルで固定しない。**
- **見どころは日本語のときだけ出す**。`highlight` は日本語データしかなく、本文が
  他言語のときに見どころだけ日本語だと不整合になる。多言語データが入り次第この条件は外す。
- 解説は本文中の改行を尊重して段落に割る。1行は全角35文字程度で頭打ちにし、行間を広くとる。
  最長772文字を読ませる画面なので、走査用の組みでは持たない。
- **前後の作品へ移動できるようにし、リンクに相手の題名を入れる**。ラッセランドは
  小屋番号順に並んでおり、来場者は物理的に隣の小屋へ歩く。一覧に戻らせない。
- **タグは詳細では操作要素にしない**。絞り込みは一覧の仕事で、詳細では分類の表示に徹する。

**一覧から詳細へは題名だけをリンクにし、擬似要素で行全体を覆う。** 行ごと `<Link>` で
包むと画像・団体名・制作者名・抜粋の全部が読み上げの対象になる。アクセシブルネームは
作品名だけに保ちつつ、タップ領域は行全体を取る。

## Language

**6言語すべてをHTMLに描き、CSSで表示を切り替える。** 静的書き出しであることと、
会場の電波が混雑することの両方から出る帰結。

- **切替に通信を発生させない。** 利用者は「読めない」状態で切替を押す。そこで待たせたら、
  何が起きたのか確認する手段が本人には無い。
- **初回描画の前に言語を確定する。** 日本語が一瞬見えてから切り替わる挙動は、
  自分の言語で読めるという確信を毎回削る。判定は `<head>` の同期スクリプトで行う。
- **ブラウザの言語から自動で選ぶが、その結果は保存しない。** 保存するのは利用者が
  明示的に選んだときだけ。自動判定を保存すると、端末を借りた別の人にまで引き継がれる。
  やさしい日本語は自動では選ばない。本人が選ぶ言語である。
- **ボタンのラベルはその言語自身の表記にする**（한국어であって「韓国語」ではない）。
  自分の言語を探している人は、自分の言語の字面を探している。
- **言語ブロックには `lang` 属性を付ける。** 読み上げの発音がそこで決まる。
- 切替は6言語を並べて全部見せる。折りたたむと、探している言語がここにあると分からない。

**翻訳する文言と、しない文言の線を引く。**

- **翻訳するのは、こちらが著述した文言だけ**（見出し・ボタン・ラベル・注記）。
  題名・読み仮名・団体名・制作者名・タグ値・解説本文は**データ側の日本語**であり、
  翻訳も書き換えもしない（PRODUCT.md 掲載条件）。
- **翻訳しない日本語には `lang="ja"` を付ける。** `<html lang="ko">` のまま日本語を置くと、
  読み上げが韓国語の発音規則で固有名詞を読む。掲載条件で必ず載せる団体名・制作者名・題名の
  3点こそ、最も誤読させてはいけない。
- **`alt` や `aria-label` はCSSで切り替わらない。** これらに文言を持たせず、
  切り替わる要素へ逃がす。画像の `alt` は題名だけにし、「原画」の表記は
  読み上げに残る `figcaption` が各言語で担う。`aria-label` は
  `aria-labelledby` に置き換え、参照先を切り替わる見出しにする。
- ページの `<title>` は日本語のまま。静的書き出しでは1URLに1つしか持てない。

## Filtering

**OR方式。** 選択したタグの**いずれか**を持つ作品を出す。AND方式は23件という母数に対して
0件を量産し、絞り込みが「探す道具」ではなく「失敗する道具」になる。

- 何も選んでいない状態＝全23件。これを既定とし、サーバー描画と初期表示を一致させる。
- **既定は折りたたみ。** タグ値は4軸で27種類あり、常時展開すると一覧に到達するまでの
  スクロールが増える。閉じていても**選択中のタグは常に見せる**。行からタグを外した以上、
  「いま何で絞られているか」を画面から失わせない。
- 各タグに**件数を併記**する。1件しかないタグを選ばせてから気づかせない。
- 選択状態は塗りの有無で表す。軸の色だけに意味を持たせない。
- タップ領域は最低44px。片手操作を前提とする。

**行はサーバー描画のまま渡す。** 絞り込みのためにクライアントへ全データを渡すと、
6言語の本文がRSCペイロードに二重に載り index.html が110KB→約260KBになる。
クライアントには通し番号とタグだけの索引（約2.9KB）を渡し、描画済みの行を
children として絞り込む。会場の電波を前提にすれば、この差は体感に直結する。

## Motion

動きは状態の変化を伝えるためだけに使う。装飾的な動きを持たない。

- `prefers-reduced-motion: reduce` を尊重する。この場合、位置と不透明度の変化を
  取り除き、状態の差は色と輪郭だけで表す。
- 触れた瞬間に応答を返す。会場は電波が混雑するため、体感速度が信頼に直結する。
  押下のフィードバックは pointer-down で出し、遅延を挟まない。

## Terminology

- 画像は**「原画」**と表記する。UI文言・見出し・代替テキスト・ファイル名・
  データのキー名すべてに適用する（PRODUCT.md 掲載条件2）。
- **提供された解説本文は一切書き換えない。** 本文中に「下絵」があってもそのまま
  表示する。同条2の但し書きを参照。
- タグ軸の見出しは、スプレッドシートの列名ではなく**中身に合わせた名前**を使う。
  データ側の列名は変更しない。

| CSV列名 | 表示見出し |
|---|---|
| タグ1（テーマ） | テーマ |
| タグ2（メッセージ） | 題材 |
| タグ3（シーン） | 場面 |
| タグ4（時代背景） | 時代・出典 |
