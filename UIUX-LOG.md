# UI/UX改善・レスポンシブ対応ログ

2026-07-26 着手。apple-design スキル本文（`~/.claude/skills/apple-design/SKILL.md`、17章）を
実際に開いて参照している。章の要約では代用しない。

ブレークポイント：**スマホ 〜499px（基準）／タブレット 500〜1024px／デスクトップ 1025px〜**。
会場での主要利用シーンはスマホ。タブレット・デスクトップは余白と情報密度の調整で対応する。

---

## Before 実測（着手時点）

計測方法：Chrome DevTools Protocol。各画面 × 3幅（390 / 768 / 1440px）。
「全角1行文字数」は実測した1文字あたりの幅から算出。

| 画面 | 幅 | h1 | 本文 | 全角1行 | 最小タップ高 | 横あふれ |
|---|---|---|---|---|---|---|
| 一覧 | 390 | 32px / 行間1.15 / 字間**+0.64px** | 13px / 1.7 | 26字 | **23px** | 0 |
| 一覧 | 768 | 60px / 1.15 / 字間**+1.2px** | 13px / 1.7 | **51字** | **32px** | 0 |
| 一覧 | 1440 | 60px / 1.15 / 字間**+1.2px** | 13px / 1.7 | **77字** | **32px** | 0 |
| 詳細 | 390 | 28px / 1.3 / +0.56px | 13px / 1.7 | 22字 | 44px | 0 |
| 詳細 | 768 | 44px / 1.3 / +0.88px | 13px / 1.7 | 40字 | 44px | 0 |
| 詳細 | 1440 | 44px / 1.3 / +0.88px | 13px / 1.7 | 40字 | 44px | 0 |
| 地図 | 390 | 32px / 1.15 / +0.64px | 16px / 1.7 | 22字 | **15px** | 0 |
| 地図 | 768 | 60px / 1.15 / +1.2px | 16px / 1.7 | 44字 | **15px** | 0 |
| 地図 | 1440 | 60px / 1.15 / +1.2px | 16px / 1.7 | 44字 | **15px** | 0 |

### 確定した欠陥

1. **大見出しの字間が正** — 60pxで `+1.2px`。第15章は「大きな表示用テキストは**負**の字間」。符号が逆。
2. **一覧の抜粋が1行77文字**（デスクトップ）／51文字（タブレット）。
   `DESIGN.md` は「1行は全角35文字程度で頭打ち」。読める行長を超えている。
3. **タップ領域が44px未満** — 地図15px（Leafletの帰属リンク）、一覧23〜32px。
   第16章 原則5（Flexibility）。片手操作が前提。
4. **`prefers-contrast: more` / `prefers-reduced-transparency` が0件** — 第14章。
   屋外・直射日光下が既定条件のこのアプリでは、3信号のうち contrast が最も効く。
5. **`prefers-reduced-motion` が全要素 `0.01ms` の一律停止** — 第14章が名指しで否定する書き方。
   「reduced motion は動きを消すことではなく、前庭感覚を刺激しない等価物に置き換えること」。
6. **余白の px 固定 62件 / rem・em 8件** — 第15章の Dynamic Type 対応が抜けている。
7. **ブレークポイントが場当たり** — `760px` / `499px` / `519px` が混在。

---

## 変更ログ

### 0. 土台（`app/globals.css`）

| 項目 | Before | After | 章 |
|---|---|---|---|
| `prefers-reduced-motion` | `*` に `transition-duration: 0.01ms !important` | 対象を列挙し、**色・不透明度の変化は残す**。`:active` の位置移動のみ色変化に置換 | 14 |
| `prefers-contrast: more` | なし | `--rule: #5b554b`。地に対し **2.51:1 → 6.50:1** | 14 |
| `prefers-reduced-transparency` | なし | 対応なし（`backdrop-filter` が実在しないため受け皿を作らない判断） | 14 |
| 大見出しの字間 | `+1.2px`（60px時） | `h1 { letter-spacing: 0 }` ＋ `h1 [data-lang-block="en"] { -0.02em }` | 15 |
| 見出しの行間 | 1.15 / 1.3 | 1.1 / 1.2（大きいほど詰める） | 15 |
| 余白の単位 | px **62件** / rem・em 8件 | px **0件** / rem・em **72件** | 15 |
| ブレークポイント | `760` / `499` / `519` が混在 | `min-width: 500` / `min-width: 1025` のスマホファースト2段 | 16-5 |

**字間のCJK問題の解き方**：6言語が同一DOMに同居するため、`h1` に一律で負の字間を与えると
和文・中文・韓文にも及んでしまう。負の字間を**英語ブロックにだけ**効かせることで回避した。
`DESIGN.md`「CJK本文に負の字間を与えない」を満たす。

**検証**：既定16pxでの描画は Before と一致（h1 32/60/28/44px、本文13px/行間1.7、
全角1行文字数の差は ±3字以内で字間変更由来）。`tsc` エラー0。

### ⚠ Before計測の訂正

着手時に記録した「一覧の抜粋が1行78字」は**誤り**だった。計測スクリプトが
`main p` を先に拾い、実際には `.image-note`（原画の注記）を測っていた。
正しくは：

| 要素 | 390 | 768 | 1440 |
|---|---|---|---|
| `.entry-excerpt`（抜粋） | 21字 | 37字 | 32字 ← **問題なし** |
| `.image-note`（注記） | 26字 | 54字 | **78字** ← これが実際の欠陥 |
| `.detail-body p`（解説） | 22字 | 35字 | 35字 ← 適正 |
| `.map-guidance`（案内文） | 22字 | **44字** | **44字** ← 要修正 |

同様に「一覧の最小タップ高23px」も、リンク要素の箱の高さであって
**実効タップ領域ではなかった**（擬似要素が行全体を覆う設計のため）。

---

### 1. 一覧画面（`app/globals.css`）

| 項目 | Before | After | 章 |
|---|---|---|---|
| `.image-note` の行長 | 上限なし → 1440pxで**全角78字** | `max-width: 35em` → **34字** | 15 |
| 行の実効タップ領域 | 未検証 | 5×5格子で **25/25被覆**（390・1440とも） | 16-5 |
| タブレット本文列 | `minmax(0, 1fr)` | `minmax(0, 35rem)` | 16-7 |
| デスクトップ列構成 | `40 / 112 / 352 / 476px` | `40 / 150 / 350 / 428px` | 16-5 |
| デスクトップのサムネイル | 112px | **150px**（素材450px幅・2倍DPRで300px≤450pxのため破綻しない） | 16-7 |
| 行の高さ（1440） | 179px | **158px** | 16-6 |

スマホ（〜499px）の描画は**意図的に不変**。会場での主要シーンがスマホであるため、
基準を動かさずタブレット・デスクトップだけを調整した。

掲載条件（団体名・制作者名・題名の3点）は全ブレークポイントで無条件描画のまま。
省略・折りたたみ・`text-overflow` は導入していない。

### 2. 詳細画面（`app/globals.css`）

**確定した欠陥**：デスクトップ（1025px〜）向けの調整が**一つもなかった**。768pxと1440pxで、
左右パディング（18px→32px）以外まったく同一の描画になっていた。
`.page` は `min(100%, 1120px)` で中央寄せされる一方、その中身（`.detail-header` 40rem /
`.detail-artwork` 640px / `.detail-body` 35em）は**すべて左寄せ**。結果、1120pxのうち
**右側480pxが完全な死に余白**になり、読み物本体が画面全体から見て大きく左に寄っていた。

| 項目 | Before | After | 章 |
|---|---|---|---|
| デスクトップの `main` 幅 | 1120px（中身は左寄せ、右に480pxの死に余白） | **704px を中央寄せ**（コンテンツ640px） | 16-7 |
| デスクトップの本文カラム | 左端に固定 | 画面中央（左右マージン各368px） | 16-6 |
| `.detail-pagination` の幅 | `max-width: 760px` → 768pxで**732px**（上の内容より92px広い） | `40rem` → **640px**、上のコンテンツと右端が揃う | 16-7 |
| `overflow-wrap` | `anywhere`（不要な位置でも単語を分断） | `break-word`（あふれる語だけ折り返す） | 15 |

`width: min(100%, 40rem)` では `box-sizing: border-box` のため左右パディングが含まれ、
デスクトップのコンテンツが **576px とタブレット（640px）より狭くなる逆転**が起きた。
パディング分（2rem×2）を足した `44rem` に補正して解消。

**検証**（390 / 768 / 1440px 実測）：
- スマホ390px：ページ高 2501.7px・本文354px（22字）・pager 354px — **Before と完全一致**（意図どおり不変）
- タブレット768px：本文640px（35字）— 不変。ただし**pager だけは 732px → 640px に変わった**
  （上の内容と揃える意図的な変更。指示上は「タブレット不変」としていたが、
  760px上限に届いていた分だけ影響した。悪化ではなく整列の改善）
- デスクトップ1440px：`main` 704px 中央寄せ、本文640px（35字）
- 全幅で横あふれ 0、タップ領域は戻る117.6×44 / 言語切替6件すべて44 / pager 64px
- `tsc` エラー0、`npm run build` 成功（25 HTML / 17 JS / 2 CSS）

**誤検出として記録**：`.detail-tag` は 54×37.8px で44px未満だが、これは `<li>` であって
**リンクでもボタンでもない**（表示専用）。タップ領域要件の対象外なので修正しない。

### 3. 地図画面（`app/globals.css`）

**確定した欠陥（最優先）**：小屋リストの行がほぼタップできなかった。
5×5格子で `elementFromPoint` を撃った実効タップ被覆は **390pxで0/25・768pxで1/25・1440pxで0/25**。
リンクだったのは題名の 106.7×**21px** だけで、小屋番号・団体名・制作者名の領域はすべて素通り。
会場での片手スマホ操作が前提の画面としては致命的（第16章 原則5 Flexibility、第10章 Tap）。

| 項目 | Before | After | 章 |
|---|---|---|---|
| リスト行の実効タップ被覆 | **0/25**（390・1440）／1/25（768） | **25/25**（全幅） | 16-5 / 10 |
| Leaflet 帰属リンクの高さ | **15px** | **25.8px**（WCAG 2.5.8 AA の24px超） | 16-5 |
| `.map-guidance` の行長 | `max-width: 44rem` → **全角44字** | `35em` → **35字** | 15 |
| `h1`（`.map-title`）| 32 / **60** / **60px** | 32 / **44** / **44px**（詳細画面と一致） | 16-4 |
| デスクトップの `main` | 1120px（中身は左寄せ） | **704px を中央寄せ**（コンテンツ640px） | 16-7 |
| `.map-hut-section` | `max-width: 52rem`（832px） | `40rem`（640px） | 16-7 |
| `.rasseland-map` | 上限なし → 768pxで**732px** | `max-width: 40rem`（640px） | 16-7 |
| `overflow-wrap` | `anywhere` | `break-word` | 15 |

**行タップの解き方**：一覧画面で既に使っている擬似要素で行全体を覆う手法をそのまま流用した
（`.map-hut-entry { position: relative; isolation: isolate }` ＋ `.map-hut-title a::after { inset: 0 }`）。
2画面で同じ手法に揃えたので、行の挙動が画面をまたいで一致する（第16章 原則4 Familiarity）。

**幅の不揃い**：1440pxで `main` 1120 / `h1` 640 / 案内文 704 / 地図 1056 / リスト 832 と
**5つの異なる幅**が根拠なく並んでいた。40rem（640px）に統一し、
本文の `.map-guidance` だけを 35em（560px）と意図的に狭くした。

**h1 の下限を 1.75rem ではなく 2rem に据えた理由**：詳細画面と同じ clamp にすると
スマホの32pxが28pxに変わってしまう。上限だけを 3.75rem → 2.75rem に下げ、
**スマホは32pxのまま**でタブレット・デスクトップだけ44pxに揃えた。

**検証**（390 / 768 / 1440px 実測）：
- スマホ390px：`main` 390 / padding 18px / `h1` 32px / 案内文22字 / 地図 354×405.1px /
  ズームボタン 44×44px / 戻るリンク 117.6×44px — **すべて Before と一致**。
  変わったのは**タップ被覆 0/25 → 25/25** と帰属リンク 15 → 25.8px だけ
- タブレット768px：`h1` 640 / 地図 640 / リスト 640 で右端が揃った
- デスクトップ1440px：`main` 704px 中央寄せ、以下すべて 640px
- 全幅で横あふれ 0、`tsc` エラー0、`npm run build` 成功

### 4. 言語切替UI（`app/globals.css`）

**確定した欠陥**：`position: static` だったため、**208pxスクロールした時点で
言語を変える手段が画面から完全に消えていた**。詳細ページは2502px、地図は5019pxある。

| ページ | 幅 | 切替UIが消えるスクロール量 | ページ全体の高さ |
|---|---|---|---|
| 詳細 | 390 | **208px** | 2502px |
| 地図 | 390 | **208px** | 5019px |
| 一覧 | 390 | 264px | 6015px |

対象は留学生・海外観光客であり、「自分の言語を見つける」がこの製品の最重要操作。
日本語を読めない利用者が共有リンクで詳細ページに着地し、少しスクロールしただけで
脱出手段を失う状態だった（第16章 原則2 Agency、原則5 Flexibility）。

| 項目 | Before | After | 章 |
|---|---|---|---|
| `position` | `static` | **`sticky; top: 0`** | 12 / 16-2 |
| 1000pxスクロール時の可視性 | **消える** | **`top: 0` で常時可視**（3ページ×3幅すべて） | 16-2 |
| スクロール後のタップ到達 | — | **6/6 OK**（地図画面でもLeafletに隠れない） | 16-5 |
| 地 | なし | 不透明な `--washi` | 12 |
| 構造の示し方 | なし | `border-bottom: 1px solid --rule-strong` | 12 |
| `scroll-padding-top` | なし | 97px（スマホ）/ 45px（500px〜） | 16-7 |
| `z-index` | なし | **1100**（Leafletのコンテナ1000・コントロール800より上） | 12 |

#### 第12章をそのまま適用しなかった理由（重要）

第12章は「ナビ／ツールバーを**半透明のレイヤー**（`backdrop-filter: blur()`）として作る」と述べているが、
**`backdrop-filter` は採用しなかった**。`DESIGN.md` に次の決定があるため：

> **構造** … 影・角丸・**ぼかし**ではなく**罫線**で作る。日中の屋外では影とぼかしが
> 最初に失われるため、輪郭で構造を伝える。

屋外・直射日光下という利用条件に基づく決定なので覆さない。
第12章の**意図**（「浮いた機能レイヤーとして作り、コンテンツをその下に流す。
不透明な帯で固定の幅を食わない」）だけを、このプロジェクトの材料
——**和紙の地と罫線**——で実装した。ガラス表現は使っていない
（`backdrop-filter: none` / `box-shadow: none` / `border-radius: 0px` を実測で確認）。

副次的な効果として、`prefers-reduced-transparency` の受け皿を作る必要がなくなり、
土台タスクでの「半透明を使わないので対応しない」という判断と整合が取れた。

#### レビューで見つかった問題と修正（safe-area）

Codexレビューの [P2]：`top: 0` のままだと、**PWAをiPhoneでスタンドアロン起動したとき、
スクロール後に言語ボタンがノッチ／ステータスバーの領域に入り込む**。
`.page` は初期状態では `env(safe-area-inset-top)` を padding で考慮しているが、
sticky バーはそれを踏み越えてしまう。会場でホーム画面から起動する前提の製品なので実害がある。

- `top: 0` → `top: env(safe-area-inset-top, 0px)`
- ノッチ領域に地が抜けないよう、固定擬似要素で上端を `--washi` で覆う（バーの寸法には影響しない）
- `scroll-padding-top` に `env(safe-area-inset-top, 0px)` を加算

**検証の限界（正直に記録）**：インセット0の環境では上記変更後も実測値が
**すべて変更前と同一**（バー97/45px、`scroll-padding-top` 97/45px、ボタン44px、
`z-index` 1100、背景 `rgb(245,240,228)`）であることを確認した。
一方、**ノッチのある実機でのスタンドアロン表示は計測できていない**。
CDPでは `safe-area-inset` を実機同等に再現できないため、この点はコードレビュー止まりで、
実機確認は未実施。

#### 残るトレードオフ（現状維持で確定）

スマホでは6言語が2行に折り返すため、固定バーが **97px＝画面の11.5%を常時占有**する。
「やさしい日本語」が138pxと最も長く、354pxの幅に6つを1行で収めることはできない。
横スクロール・ドロップダウン・折りたたみは**選択肢を隠す**ことになり、
「自分の言語を見つける」という最重要操作を損なうため採らなかった。
11.5%を払う価値があるかは判断が分かれるが、**全部見せる方を優先すると確定した**
（2026-07-26 隼真さん）。折りたたみ・ドロップダウン化は今後も提案しない。

**検証**（一覧・詳細・地図 × 390 / 768 / 1440px＝9通り）：
- 1000pxスクロール後も9通りすべてで `top: 0` に可視
- スクロール後に `elementFromPoint` で6ボタンすべてに到達（**6/6 OK**）——
  地図画面でもLeafletのズームボタン・帰属表示に隠れない
- スクロール後に English を押して `data-lang` が `en` に変わり、
  表示中の言語ブロックが詳細で14/91に切り替わることを確認
- ボタンは全幅・全ページで44px高を維持（74 / 138 / 82.6 / 90 / 90 / 67.5 px）
- `tsc` エラー0、`npm run build` 成功

### 5. 選択中タグチップに灯りを入れる（`app/globals.css`）

視覚システムに第三のレイヤー**「光」**を重ねる作業の1件目。
世界観「和紙と墨」は置き換えず、**「Washi, Sumi and Light」**とする。
ねぶたは内側から光る張り子だが、それまでのUIには灯りが入っていなかった。

**採用した原則**：

> **状態は縁で、雰囲気は階調で。**

光でシステムの**状態**を伝える箇所は、面のグラデーションではなく**ぼかし半径0の硬い縁**で表す。
`DESIGN.md` が「日中の屋外では影とぼかしが最初に失われる」と定めており、
面の階調は直射日光下で最初に消えるため、状態の伝達には使えない。
雰囲気（ヒーロー背景・原画の背後）は見えなくても何も失われないので、そこでは階調を使ってよい。

| 項目 | Before | After | 章 |
|---|---|---|---|
| トークン | なし | **`--lamp: #ffcf8a`**（和紙を透かした電球色） | 12 |
| 選択中チップの塗り | `var(--tag-color)` | **変更なし** | — |
| 選択中チップの文字色 | `var(--on-axis)` | **変更なし** | — |
| 灯り | なし | **`box-shadow: inset 0 2px 0 0 color-mix(in srgb, var(--tag-color) 55%, var(--lamp))`** | 12 |
| `prefers-contrast: more` | 灯りなし | **`box-shadow: none`**（中間色の線は輪郭を鈍らせるため消す） | 14 |

#### 文字コントラストは1ポイントも動いていない（実測）

文字が乗る面の色に触れていないため、白文字のコントラストは変化しない。
ブラウザで実際に描画された値を読んで確認した。

| 軸 | 塗り | 灯線（実測） | 白文字/塗り Before | After | 灯線と塗りの差 |
|---|---|---|---|---|---|
| 藍 テーマ | `#27436f` | `#88827b` | 9.91 | **9.91** | 2.61 |
| 緑青 題材 | `#1f5b4e` | `#848f69` | 7.88 | **7.88** | 2.29 |
| 朱 場面 | `#96372b` | `#c57b56` | 7.30 | **7.30** | 2.20 |
| 山吹 時代・出典 | `#725000` | `#b1893e` | 7.34 | **7.34** | 2.28 |

灯線と塗りの差が 2.20〜2.61 に収まっており、**4軸で灯りの見え方がほぼ揃う**。
軸によって光の強さが変わって見えることはない。

#### `color-mix` のフォールバックを書かなかった理由

非対応環境では `box-shadow` の宣言だけが無効になり、**平らな塗りがそのまま残る**。
選択状態は塗り（`background: var(--tag-color)`）と白文字が担っており、
光はその上乗せにすぎないので、灯りが出なくても状態は失われない。
分岐を増やさず、宣言順だけ `background` / `color` の後に置いた。

#### 灯りが枠の内側に入る（意図どおり）

`.filter-tag` は `border: 2px solid var(--tag-color)` を持つ。inset の影は境界線の**下**に描かれるため、
上辺は「2pxの枠 → 2pxの灯り」の順に重なり、灯りが枠の内側から覗く形になる。
`DESIGN.md` の「**枠の内側から透ける**濃彩の染料」と同じ組み方になった。

**検証**：
- 実描画の `background` は4軸とも変化なし（`rgb(39,67,111)` / `rgb(31,91,78)` / `rgb(150,55,43)` / `rgb(114,80,0)`）
- 展開時の `.filter-tag[aria-pressed="true"]` と折りたたみ時の `.filter-tag-selected` の**両方**に灯りが出る
- `prefers-contrast: more` をエミュレートし、**両方で `box-shadow: none`** になることを確認
- `.detail-tag`（詳細画面のタグ）には灯りを入れていない（分類の表示であって状態ではない）
- `tsc` エラー0、`npm run build` 成功、生成CSSに3つの規則すべてが載ることを確認
- 検出器の指摘は既知の1件（`.map-guidance` の左4px罫、棄却済み）のみで**新規の指摘なし**

### 6. 一覧行のタップフィードバックを作り直す（`app/globals.css`）

「Washi, Sumi and Light」の2件目。**これは見た目の調整ではなく、アクセシビリティ欠陥の修正である。**

#### 見つかった欠陥

変更前の `.catalog-entry:active` は罫線用のトークン `--rule` を**面の塗り**に使っていた。

```css
.catalog-entry:active { background: var(--rule); }
```

`.catalog-entry` の中の文字はほとんどが `--sumi-weak`（#5b554b）である
（`.entry-number` / `.title-reading` / `.entry-credits` / `.entry-excerpt`）。

| 押下時の面 | 墨 #211f1a | 墨弱 #5b554b | |
|---|---|---|---|
| `--rule` = #aaa191（通常時） | 6.44 | **2.89** | ✗ 下限割れ |
| `--rule` = #5b554b（`prefers-contrast: more` 時） | 2.23 | **1.00** | ✗ 文字が消える |

2つめが深刻だった。`@media (prefers-contrast: more)` は `--rule` を `#5b554b` に上書きしており、
これは `--sumi-weak` と**同一の値**である。つまり
**高コントラスト表示を要求している利用者が行をタップすると、その行の文字が完全に見えなくなっていた。**
コントラストを最も必要としている人にだけ最悪の結果が出ていた。

どちらの記述も単独では正しく、2つが出会ったところにだけ欠陥がある。
`--rule`（線のための色）を面の塗りに流用したことが原因である。

#### Before / After

| | Before | After |
|---|---|---|
| 面 | `--rule` #aaa191 | **`--washi-lit` #faedd5**（新トークン） |
| 下辺の罫 | `--rule` #aaa191 | **`--rule-strong` #39352e**（既存トークン） |
| 墨 #211f1a のコントラスト | 6.44 | **14.22** |
| 墨弱 #5b554b のコントラスト | **2.89** | **6.37** |
| 高コントラスト時の墨弱 | **1.00** | **6.37** |
| 罫の見え方（面に対して） | 2.51 | **10.53** |

#### 面と縁の2チャンネルにした理由

原則は 1件目と同じ **「状態は縁で、雰囲気は階調で。」**

面だけを変えると、直射日光下では階調が最初に飛ぶため押した感触が失われる。
硬い1pxの罫は日光下でも残る。そこで、
**面（暖まる）＝雰囲気／罫（締まる）＝状態**の2つで伝える。

`--rule-strong` は原画サムネイルの枠（`.entry-image img`）と同じトークンなので、
押されている行の下辺が、隣にある原画の枠と同じ墨の重さに揃う。

面の推移は次のとおり。ホバーが「沈む」のに対し、押下は「暖まる」——色相を第2チャンネルにした。

| 状態 | 色 | surface からの輝度差 |
|---|---|---|
| 通常 `--surface` | #fffdf7 | — |
| `:hover` `--washi` | #f5f0e4 | 11.1% 沈 |
| `:active` `--washi-lit` | #faedd5 | **12.8% 沈**（＋暖色へ寄る） |

#### `--washi-lit` を `color-mix()` で定義しなかった理由（1件目と判断が違う）

カスタムプロパティの値が `color-mix()` 未対応環境で無効になると、
それを参照する `background` は「初期値」＝ `transparent` に落ちる。
**平らな塗りに戻るのではなく背景が消える**ため、フォールバックとして成立しない。

1件目のチップで `color-mix()` を使えたのは、あれが `box-shadow` の値に直接書かれていて、
無効化されればその宣言だけが落ちる形だったからである。ここは事情が違うので16進数リテラルで書いた。

#### 検証（CDPで実描画を測定）

- 通常時：`rest` #fffdf7 → `:hover` #f5f0e4 → `:active` **#faedd5**、下罫 **#39352e**
- `prefers-contrast: more`：`:active` は同じ値になり、**1.00:1 の消失は解消**
- **押下時に矩形が1pxも動かない**（`[18,447,354,240]` が3状態とも同一）。
  `border-bottom-width` は 1px のまま、`border-radius: 0px` / `box-shadow: none` を維持
- 通常時の `transition-duration` は **0s**（pointer-down で即時。`DESIGN.md` の規定どおり）
- `prefers-reduced-motion: reduce` のときだけ `background-color` と `border-bottom-color` が
  ともに 0.1s で追従し、正しく #faedd5 / #39352e に着地することを確認
- `tsc` エラー0、`npm run build` 成功、生成CSSに新トークン・`:active`・transition の3つとも載る
- Codexレビューゲート通過（指摘なし）
- 検出器の指摘は既知の1件（`.map-guidance` の左4px罫、棄却済み）のみで**新規の指摘なし**

### 7. タイポグラフィ 前半（`app/globals.css`）

拠り所は Apple *The Details of UI Typography*（WWDC 2020）の2点。

> トラッキングはサイズ固有であり、全サイズに1つの値を当ててはならない。
> 大きな見出しは負を、小さな文字はわずかに正を求める。

> レディングはサイズに反比例する。大きな見出しは詰め、本文は緩める。

#### 7-1. 英語のトラッキングをサイズごとに出し分ける

言語別のトラッキング指定は `h1` の1か所にしかなく、他の見出しは全言語同値だった。

| セレクタ | サイズ | Before（全言語） | After 英語 | After CJK・韓 |
|---|---|---|---|---|
| `h1` | 32px | `-0.02em`（既存） | `-0.02em` | `0` |
| `.map-title` | 32px | 指定なし | **`-0.02em`** | `0` |
| `.map-hut-section h2` | 22.4px | `+0.01em` | **`-0.01em`** | `+0.01em` |
| `.detail-highlight/body/tags h2` | 20px | `+0.01em` | **`-0.01em`** | `+0.01em` |
| `.filter-axis h3` | 16px | `+0.02em` | **`0`** | `+0.02em` |
| 小さなラベル各種 | 13〜15px | `+0.04em` | `+0.04em` | `+0.04em` |

CJKの正の値は**意図的なもの**なので据え置いた（漢字・仮名の字間をわずかに開けると
小さいサイズで読みやすい）。小さなラベルの `+0.04em` も原則どおり正しいので触っていない。

**負のトラッキングをCJK・ハングルに当ててはならない。**
全角の枠に詰めて設計された字形は、詰めると字面が接触する。

#### 英語専用の指定に `:lang(en)` を使わない理由

言語切替は `document.documentElement.lang` を書き換える。英語表示中は `<html lang="en">` になるため、
`lang` 属性を持たない要素はすべて `:lang(en)` に一致する。
作品名は常に日本語だが、それを包む `h2` には `lang` が無い。
`:lang(en)` で書くと**中身が日本語のままの見出しに英語用の詰めが当たる。**

`data-lang-block` は6言語すべてを持つUI文言にだけ付いているので、
`[data-lang-block="en"]` なら英語のテキストだけに確実に当たる。既存の `h1` の書き方に揃えた。

**実測**（作品名 `.entry-title h2 a`、`lang="ja"` 固定）:
6言語すべてで `letter-spacing: 0.2px` のまま。負の詰めは一度も当たっていない。

#### 7-2. 韓国語の分かち書きを保護する

韓国語は語（어절）の間に空白を置くが、既定では**語の途中で改行されていた**。

```css
:lang(ko) { word-break: keep-all; overflow-wrap: break-word; }
```

ここは `:lang()` を使う。フォント選択と同じく**実際の文字の言語に従うべき指定**だからである。
`overflow-wrap: break-word` は安全弁。`anywhere` は `min-content` 幅の計算に影響するので使わない。

#### レビュー前に実測で見つけた欠陥（`word-break` の継承漏れ）

**`word-break` は継承されるプロパティである。** 韓国語を選ぶと `<html lang="ko">` になり、
`lang` を持たない `body` / `.page` が `:lang(ko)` に一致して `keep-all` を受け取り、
**その値が継承で `lang="ja"` の要素の中まで降りていた。**
`:lang(ja)` 側に対抗する宣言が無いため継承値が勝つ。

| 要素（`lang="ja"`） | `data-lang=ko` のとき |
|---|---|
| `.detail-header h1` | `keep-all` ← 誤り |
| `.map-hut-title a` | `keep-all` ← 誤り |
| `.entry-title h2 a` | `keep-all` ← 誤り |

日本語はほぼ任意の位置で改行できるが、`keep-all` では空白でしか折れない。
横溢れは `overflow-wrap` が受け止めるので出ないが、改行位置が不自然になる。

7-1 で `:lang(en)` の罠を避けておきながら、`word-break` も継承プロパティであることを見落とした。
次を `:lang(ko)` の直後に足して解消した（詳細度が同じなので記述順で決まる）。

```css
:lang(ja), :lang(zh-Hans), :lang(zh-Hant) { word-break: normal; }
```

`:lang(en)` は足していない。ラテン文字では `normal` と `keep-all` の挙動が実質同じだからである。

**修正後の実測**（一覧・詳細・地図の3画面 × 6言語）:
`keep-all` が付くのは韓国語のテキストだけ。日本語の作品名は**全言語で `normal`**。
横幅は全18通りで 390px、横溢れなし。

#### 7-3. `.map-title` のレディング

`font-size: clamp(2rem, 8vw, 2.75rem)` に対して `line-height` の指定が無く、
`body` の `1.7` を継いでいた。44pxの見出しに75pxの行送り。
同格の `h1`（1.1）・`.detail-header h1`（1.2）に揃えて `1.2` にした。

**実測**: 54.4px → **38.4px**。

#### 検証

- `tsc` エラー0、`npm run build` 成功
- Codexレビューゲート通過（指摘なし）
- 検出器の指摘は既知の1件（`.map-guidance` の左4px罫、棄却済み）のみで**新規の指摘なし**

#### 後半（別タスク）

言語別フォントスタックは未着手。**简体中文・繁體中文が日本語書体で描画されている**
（下の「データ・素材側の課題」を参照）。

### 未解決（後続タスクで対応）

- ~~地図のリスト行が 0/25 被覆~~ → 3で解決（25/25）
- ~~`.map-guidance` が全角44字~~ → 3で解決（35字）
- ~~Leaflet の帰属リンクが 15px~~ → 3で解決（25.8px）
- ~~第12章（素材と奥行き）が未適用~~ → 4で対応（ガラスではなく和紙＋罫線として）
- ~~スマホの固定バーが画面の11.5%を占有~~ → **現状維持で確定**（下記）
- ~~`DESIGN.md` との差分（112px対150px）~~ → **150pxを採用しDESIGN.mdを更新**（下記）
- ~~`.map-guidance` の左4px罫を検出器が警告~~ → **棄却で確定**（下記）

### 確定した判断（2026-07-26 隼真さん）

監査で挙げた「要判断」3件について方針が出たので、ここで確定させる。
以後この3点を「未解決」として蒸し返さないこと。

| 論点 | 判断 | 根拠と記録先 |
|---|---|---|
| `.map-guidance` の左4px罫線 | **維持。検出器の指摘は棄却** | 罫線で構造を作るという世界観に基づく意図的な表現。`DESIGN.md` の World に棄却理由を明記した |
| スマホの固定バー2行折り返し（11.5%占有） | **現状維持** | 折りたたんで言語を隠すより、6言語を全部見せる方を優先する。`DESIGN.md` の Language の方針どおり |
| 一覧の原画 デスクトップ150px | **150pxを採用。`DESIGN.md` を実態に更新** | 実測で拡大表示0件。DESIGN.md の記述を固定値ではなく「表示幅 × DPR ≤ 素材幅450px」という式に書き換えた |

3点目について補足する。DESIGN.md を「デスクトップは150px」と書き換えるだけでも
食い違いは消えるが、それでは**なぜ112pxだったのかが失われる**。
実際に効いているのは表示幅そのものではなく、表示幅と端末のDPRの積が
素材の450pxを超えないことなので、DESIGN.md にはその式を残した。
高解像度データが入れば上限が自動的に上がり、数値の書き換えが不要になる。

### 油布さんへの確認待ち（独断で決めない）

- **原画の高解像度データの有無** — 素材が450×255pxしかなく、詳細画面では
  タブレット・デスクトップで **640px 幅に拡大表示**（1.42倍）、
  スマホでも 354px × DPR3 = **1062px 相当（2.36倍）**に引き伸ばしている。

  ただしこれは**欠陥ではなく `DESIGN.md` に記録済みの意図的な判断**：

  > **原画は画面幅いっぱいに出す**（上限640px）。一覧とは逆の判断。素材が450px幅しかなく
  > 拡大によるぼけが出るが、詳細は作品を見に来た画面であり、小さく正確であることより
  > 大きく見えることを優先する。**高解像度データが入り次第、ファイルを差し替えるだけで
  > 済むよう、表示寸法をピクセルで固定しない。**

  よって画像まわりのサイズ方針は触らない。高解像度データが得られたら差し替えるだけでよい。

- **「Umios侫武多会」の団体名の漢字** — 2つの公式資料で字が食い違っている可能性がある。

  | 出典 | 表記 |
  |---|---|
  | 解説データのスプレッドシート（`data/nebuta-source.csv`） | Umios**侫**武多会 |
  | ラッセランド配置図（`docs/R8RasseLandMap.pdf`） | Umios**倭**武多会 に見える |

  配置図PDFを12000px相当まで拡大して字形を確認したが、`侫` と `倭` の判別を
  確信できるところまでは至らなかった。**現時点ではCSVの `侫` のまま変更していない。**

  番号の対応そのものは作品名「風神 雷神 ～瑞祥の息吹～」とねぶた師「手塚 茂樹」で
  一意に確定しているため、この字の問題が小屋番号（新15番）に影響することはない。
  影響するのは画面に出る団体名の表記だけ。掲載条件で団体名の明記が求められている以上、
  誤字のまま出すわけにはいかないので、油布さんに正しい表記を確認する。

- **ラッセランドの緯度経度** — 配置図PDFを受領したが、**PDFに座標の記載がない**ため
  `lib/rasseland.ts` の `rassetLandCenter`（40.8283, 140.7377）は暫定値のまま。
  配置図から起こせたのは模式図ベースの相対位置だけで、これは実地のナビゲーションには使えない。
  地図画面の描画方法（現在のC案＝全体1ピンを続けるか、23棟の模式図に切り替えるか）は、
  緯度経度の回答を得てからまとめて判断する。

### データ・素材側の課題（コードでは直さない）

これらは `app/globals.css` の問題ではなく、データまたはフォント環境の問題である。
**解説本文は勝手に書き換えないこと。**

#### 简体中文・繁體中文が日本語書体で描画されている

CDPの `CSS.getPlatformFontsForNode` で、実際に描画に使われている物理フォントを測定した結果:

| 言語 | 表示テキスト | 使われているフォント |
|---|---|---|
| 日本語 | 一覧へ戻る | Hiragino Kaku Gothic ProN ✓ |
| English | Back to the list | .SF NS ✓ |
| **简体中文** | 返回列表 | **Hiragino Kaku Gothic ProN** ✗ |
| **繁體中文** | 返回列表 | **Hiragino Kaku Gothic ProN** ✗ |
| 한국어 | 목록으로 돌아가기 | Apple SD Gothic Neo ✓ |

原因は `body` の `font-family`。

```
system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
"Noto Sans CJK JP", "Yu Gothic", "Hiragino Kaku Gothic ProN", sans-serif
```

**日本語フォントが総称の `sans-serif` より前にある**ため、漢字はすべて日本語書体で拾われる。
PingFang SC / TC が端末にあっても到達しない。
日中で字形の異なる漢字（直・骨・者・具・令 など）が日本語の形で中国語話者に表示される。
文字コードは正しいので文章は読めるが、中国語として見ると字形が違う。
韓国語が正しい書体に届いているのは、日本語フォントにハングルが無く総称まで落ちた結果で、偶然である。

対処は `:lang()` による言語別フォントスタック（Webフォント不要）。
**全ページの見え方が変わる変更**なので、本番稼働中に入れるかは判断が要る。**未着手。**

#### 简体中文の解説にマークダウン記号が残っている

`data/nebuta.json` を全フィールド走査した結果、**2フィールド**で `**` が本文に混入していた。

| 作品 | フィールド | 冒頭 |
|---|---|---|
| 1 | `bodies.zhHans` | `**“暂（等一下）”**是歌舞伎的著名剧目之一…` |
| 1 | `excerpts.zhHans`（本文からの派生） | 同上 |

画面には `**` がそのまま文字として出る。他の5言語・他の22作品には無い。
**解説本文なので独断で書き換えない。** 翻訳担当と原文の持ち主に確認してから直す。
