/* Generated by scripts/build-pwa.mjs. Do not edit directly. */
"use strict";

const VERSION = "270266cdf2ea6fcc";
const CACHE_PREFIX = "nebuta-guide-";
const PAGE_CACHE = `${CACHE_PREFIX}pages-${VERSION}`;
const ASSET_CACHE = `${CACHE_PREFIX}assets-${VERSION}`;
const TILE_CACHE = `${CACHE_PREFIX}tiles-${VERSION}`;
const CURRENT_CACHES = new Set([PAGE_CACHE, ASSET_CACHE, TILE_CACHE]);
const PAGE_PRECACHE_URLS = [
  "/aomori-univ-nebuta-exchange-2026/",
  "/aomori-univ-nebuta-exchange-2026/map/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/1/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/2/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/3/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/4/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/5/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/6/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/7/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/8/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/9/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/10/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/11/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/12/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/13/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/14/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/15/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/16/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/17/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/18/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/19/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/20/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/21/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/22/",
  "/aomori-univ-nebuta-exchange-2026/nebuta/23/"
];
const ASSET_PRECACHE_URLS = [
  "/aomori-univ-nebuta-exchange-2026/_next/static/cFp8DUgPvMuYdFom4vPCG/_buildManifest.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/cFp8DUgPvMuYdFom4vPCG/_clientMiddlewareManifest.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/cFp8DUgPvMuYdFom4vPCG/_ssgManifest.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/05-c3ty_6dwfk.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/09e-ks0ng-lq7.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/0_u5sxmro26xe.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/0cz1d0mv5g_q7.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/0ls11j2txjbu0.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/0n8kzvw2z_6as.css",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/14mrh2-p_w84d.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/15orcrkp-_9ct.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/1b62po1j84dqt.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/21veqh5xxb-qz.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/29-i6ri2rkcu4.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/3_32kt0d0n6yy.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/3b3k26ueuw48q.css",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/3gzxhq9iq6efl.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/433f-2fo18rp2.js",
  "/aomori-univ-nebuta-exchange-2026/_next/static/chunks/turbopack-17-_h47l2na2y.js"
];
const APP_SHELL_URL = "/aomori-univ-nebuta-exchange-2026/";
const BASE_PATH = "/aomori-univ-nebuta-exchange-2026";
const NEXT_STATIC_PREFIX = `${BASE_PATH}/_next/static/`;
const IMAGE_PREFIX = `${BASE_PATH}/images/`;
const TILE_HOSTNAME = "cyberjapandata.gsi.go.jp";
const TILE_CACHE_LIMIT = 300;

self.addEventListener("install", (event) => {
  event.waitUntil(
    (async () => {
      const [pageCache, assetCache] = await Promise.all([
        caches.open(PAGE_CACHE),
        caches.open(ASSET_CACHE),
      ]);
      await Promise.all([
        pageCache.addAll(PAGE_PRECACHE_URLS),
        assetCache.addAll(ASSET_PRECACHE_URLS),
      ]);
      await self.skipWaiting();
    })(),
  );
});

self.addEventListener("activate", (event) => {
  event.waitUntil(
    (async () => {
      const cacheNames = await caches.keys();
      await Promise.all(
        cacheNames
          .filter(
            (cacheName) =>
              cacheName.startsWith(CACHE_PREFIX) &&
              !CURRENT_CACHES.has(cacheName),
          )
          .map((cacheName) => caches.delete(cacheName)),
      );
      await self.clients.claim();
    })(),
  );
});

async function putInCache(cache, request, response) {
  try {
    await cache.put(request, response);
  } catch {
    // A cache write failure must not prevent the network response from loading.
  }
}

async function networkFirstNavigation(request) {
  const cache = await caches.open(PAGE_CACHE);

  try {
    const networkResponse = await fetch(request);

    if (networkResponse.ok && networkResponse.type !== "opaque") {
      await putInCache(cache, request, networkResponse.clone());
    }

    return networkResponse;
  } catch {
    // Fall through to the precached HTML.
  }

  const cachedResponse = await cache.match(request, { ignoreSearch: true });

  if (cachedResponse) {
    return cachedResponse;
  }

  const appShell = await cache.match(APP_SHELL_URL);

  if (appShell) {
    return appShell;
  }

  return Response.error();
}

function canCache(response, allowOpaque) {
  return response.ok || (allowOpaque && response.type === "opaque");
}

async function trimCache(cache, limit) {
  const keys = await cache.keys();
  const excess = keys.length - limit;

  if (excess <= 0) {
    return;
  }

  await Promise.all(
    keys.slice(0, excess).map((request) => cache.delete(request)),
  );
}

async function cacheFirst(request, cacheName, options = {}) {
  const cache = await caches.open(cacheName);
  const cachedResponse = await cache.match(request);

  if (cachedResponse) {
    return cachedResponse;
  }

  const networkResponse = await fetch(request);

  if (canCache(networkResponse, options.allowOpaque === true)) {
    await putInCache(cache, request, networkResponse.clone());

    if (typeof options.limit === "number") {
      await trimCache(cache, options.limit);
    }
  }

  return networkResponse;
}

self.addEventListener("fetch", (event) => {
  const { request } = event;

  if (request.method !== "GET") {
    return;
  }

  const url = new URL(request.url);

  if (request.mode === "navigate") {
    event.respondWith(networkFirstNavigation(request));
    return;
  }

  if (
    url.origin === self.location.origin &&
    url.pathname.startsWith(NEXT_STATIC_PREFIX)
  ) {
    event.respondWith(cacheFirst(request, ASSET_CACHE));
    return;
  }

  if (
    url.origin === self.location.origin &&
    url.pathname.startsWith(IMAGE_PREFIX)
  ) {
    event.respondWith(cacheFirst(request, ASSET_CACHE));
    return;
  }

  if (url.hostname === TILE_HOSTNAME) {
    event.respondWith(
      cacheFirst(request, TILE_CACHE, {
        allowOpaque: true,
        limit: TILE_CACHE_LIMIT,
      }),
    );
  }
});
